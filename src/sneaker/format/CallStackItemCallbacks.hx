package sneaker.format;

class CallStackItemCallbacks {
	/** @see `CallStackItemExtension.format` */
	public static final format = CallStackItemExtension.format;

	/** @see `CallStackItemExtension.forformatStackmat` */
	public static final formatStack = CallStackItemExtension.formatStack;

	/** @see `CallStackItemExtension.formatStackIndent` */
	public static final formatStackIndent = CallStackItemExtension.formatStackIndent;
}
